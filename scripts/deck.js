"use strict";

class Deck{
    constructor(options){
        this._el = options.element;
        /*var parentDOM = options.parentDOM;
        
        var div = document.createElement("div");
        div.className = "deckSpace";
        parentDOM.appendChild(div);
        div.addEventListener('click', this._onDeckClick);*/
    }

    _onDeckClick(event){
    };
}